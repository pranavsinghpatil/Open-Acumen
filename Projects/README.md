Larger project folders. Each project should include its own README with how to run, dependencies, and a short description.

Guidelines:
- Add `requirements.txt` or `environment` metadata for reproducibility.
- Prefer small, focused repos when a project becomes public-facing.
